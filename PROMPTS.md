# AI Prompts Used

## Research

- "how does Cloudflare Agents SDK AIChatAgent work, what's the onChatMessage signature, how does this.messages persist"
- "cloudflare agents this.sql API, how to use setState vs SQL for different data"
- "Workers AI available models, how to use createWorkersAI with streamText"

## Code Assistance

- "dark theme CSS variables for a chat + card grid layout, indigo/green accents"
- "simple markdown to HTML function in typescript, handle code blocks, bold, italic, no deps"
